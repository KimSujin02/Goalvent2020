package goalvent;

public class ProfileDAO {
	// 타임라인이랑 팔로우 리스트에 표시되는 계정주의 정보 (프로필사진 / 목표) src="";를 출력해서 코드수정과 폴더 수정이 용이하게
	// 멤버페이지에 표시되는 프로필(사진 / 목표 / 설명 / 팔로우수 / 팔로잉수)
	
	public ProfileDTO boardProfile(String id) {
		ProfileDTO dto = new ProfileDTO();
		
		return dto;
	}
	
	public ProfileDTO memberPage(String id) {
		ProfileDTO dto = new ProfileDTO();
		
		return dto;
	}

}
